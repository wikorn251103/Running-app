<?xml version="1.0" encoding="utf-8"?>
<com.google.android.material.card.MaterialCardView
    xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    xmlns:tools="http://schemas.android.com/tools"
    android:layout_width="match_parent"
    android:layout_height="wrap_content"
    android:layout_margin="8dp"
    android:backgroundTint="@color/white"
    app:cardCornerRadius="5dp"
    app:cardElevation="4dp"
    app:strokeColor="@color/black"
    app:strokeWidth="1dp">

    <androidx.constraintlayout.widget.ConstraintLayout
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:paddingStart="16dp"
        android:paddingEnd="16dp"
        android:paddingTop="8dp"
        android:paddingBottom="8dp">

        <!-- วัน -->
        <TextView
            android:id="@+id/tvDayLabel"
            android:layout_width="wrap_content"
            android:layout_height="wrap_content"
            android:text="วัน:"
            android:textSize="14sp"
            android:textColor="#666666"
            app:layout_constraintTop_toTopOf="parent"
            app:layout_constraintStart_toStartOf="parent" />

        <TextView
            android:id="@+id/tvDayName"
            android:layout_width="0dp"
            android:layout_height="wrap_content"
            android:text="จันทร์"
            android:textSize="16sp"
            android:textStyle="bold"
            android:textColor="#000000"
            android:layout_marginStart="4dp"
            app:layout_constraintTop_toTopOf="@id/tvDayLabel"
            app:layout_constraintBottom_toBottomOf="@id/tvDayLabel"
            app:layout_constraintStart_toEndOf="@id/tvDayLabel"
            app:layout_constraintEnd_toStartOf="@id/btnEdit" />

        <!-- ระยะเวลา -->
        <TextView
            android:id="@+id/tvDurationLabel"
            android:layout_width="wrap_content"
            android:layout_height="wrap_content"
            android:text="โปรแกรมซ้อม:"
            android:textSize="14sp"
            android:textColor="#666666"
            android:layout_marginTop="8dp"
            app:layout_constraintTop_toBottomOf="@id/tvDayLabel"
            app:layout_constraintStart_toStartOf="parent" />

        <TextView
            android:id="@+id/tvDescription"
            android:layout_width="wrap_content"
            android:layout_height="wrap_content"
            android:text="Easy Run 6 km."
            android:textSize="14sp"
            android:textColor="#000000"
            android:layout_marginStart="4dp"
            app:layout_constraintTop_toTopOf="@id/tvDurationLabel"
            app:layout_constraintBottom_toBottomOf="@id/tvDurationLabel"
            app:layout_constraintStart_toEndOf="@id/tvDurationLabel" />

        <!-- ประเภท -->
        <TextView
            android:id="@+id/tvActivityTypeLabel"
            android:layout_width="wrap_content"
            android:layout_height="wrap_content"
            android:text="ประเภท:"
            android:textSize="14sp"
            android:textColor="#666666"
            android:layout_marginTop="4dp"
            app:layout_constraintTop_toBottomOf="@id/tvDurationLabel"
            app:layout_constraintStart_toStartOf="parent" />

        <TextView
            android:id="@+id/tvType"
            android:layout_width="wrap_content"
            android:layout_height="wrap_content"
            android:text="Easy Run"
            android:textSize="14sp"
            android:textColor="#000000"
            android:layout_marginStart="4dp"
            app:layout_constraintTop_toTopOf="@id/tvActivityTypeLabel"
            app:layout_constraintBottom_toBottomOf="@id/tvActivityTypeLabel"
            app:layout_constraintStart_toEndOf="@id/tvActivityTypeLabel" />

        <!-- Pace Avg -->
        <TextView
            android:id="@+id/tvActivityPace"
            android:layout_width="wrap_content"
            android:layout_height="wrap_content"
            android:text="เป้าหมาย:"
            android:textSize="14sp"
            android:textColor="#666666"
            android:layout_marginTop="4dp"
            app:layout_constraintTop_toBottomOf="@id/tvActivityTypeLabel"
            app:layout_constraintStart_toStartOf="parent" />

        <TextView
            android:id="@+id/tvPace"
            android:layout_width="wrap_content"
            android:layout_height="wrap_content"
            android:text="6:30-7:00/km"
            android:textSize="14sp"
            android:textColor="#000000"
            android:layout_marginStart="4dp"
            app:layout_constraintTop_toTopOf="@id/tvActivityPace"
            app:layout_constraintBottom_toBottomOf="@id/tvActivityPace"
            app:layout_constraintStart_toEndOf="@id/tvActivityPace" />

        <!-- Edit Button -->
        <ImageButton
            android:id="@+id/btnEdit"
            android:layout_width="40dp"
            android:layout_height="40dp"
            android:background="?attr/selectableItemBackgroundBorderless"
            android:src="@drawable/ic_edit"
            android:tint="@color/text_tertiary"
            app:layout_constraintTop_toTopOf="parent"
            app:layout_constraintEnd_toEndOf="parent"
            tools:ignore="UseAppTint" />

    </androidx.constraintlayout.widget.ConstraintLayout>

</com.google.android.material.card.MaterialCardView>